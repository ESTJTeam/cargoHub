server:
  port: ${GATEWAY_SERVER_PORT}
spring:
  application:
    name: gateway-server
  config:
    import: optional:file:${user.dir}/gateway/.env[.properties]
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
          routes:
            - id: user-service
              uri: lb://user-service
              predicates:
                - Path=/v1/user/**,/v1/master/**,/v1/delivery-admin/**

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVICE_URL}


custom:
  jwt:
    secrets:
      appkey: ${SECRET_APPKEY}

